*.iml
.gradle
/local.properties
/.idea
/build
/app/build
/captures
.externalNativeBuild
.cxx
local.properties

# Release-key signing, kept on the maintainer's machine, never on
# GitHub. The committed `keystore.properties.example` shows the format
# anyone rebuilding their own APK should follow.
keystore.properties
*.jks
*.keystore
